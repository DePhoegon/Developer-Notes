The values are more modern like design allowing easier updating

```GRADLE
# Sets default memory used for gradle commands. Can be overridden by user or command line properties.  
# This is required to provide enough memory for the Minecraft decompilation process.  
org.gradle.jvmargs=-Xmx3G  
org.gradle.daemon=false  
  
mod_id= < Mod ID Here >
mod_name= < Mod Name Here >  
mod_desc= < Mod Description Here >  
mod_ver= < Mod Version Here - #.#.# >
mod_group= < Mod Group Here - com.dephoegon.identical_to_mod_ID >
mod_author= < Mod Author Here, Does not support multiple >

# Do not Change these.  These are the lastest values that will properly work on this setup.
# The latest Forge Version is .2860 - This is using Forge Gradle 3+ & does not support 2.3_Snapshot
mc_chan=snapshot  
mc_chan_ver=20171003-1.12  
mc_map_ver=snapshot_20171003  
forge_version=14.23.5.2768  
  
mc_version=1.12.2
```